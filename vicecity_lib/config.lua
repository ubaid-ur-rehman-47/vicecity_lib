ViceCityConfig = {
    debug = false,
    strict = false,
    providerTimeout = 5000,
    providers = {
        framework = 'auto',
        inventory = 'auto',
        database = 'auto',
    },
    frameworkPriority = { 'qbox', 'qb', 'esx', 'standalone' },
    disabled = {},
}
